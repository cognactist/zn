import { InputFormRegDirective } from './input-form-reg.directive';

describe('InputFormRegDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFormRegDirective();
    expect(directive).toBeTruthy();
  });
});
