import { InputRegDirective } from './input-reg.directive';

describe('InputRegDirective', () => {
  it('should create an instance', () => {
    const directive = new InputRegDirective();
    expect(directive).toBeTruthy();
  });
});
