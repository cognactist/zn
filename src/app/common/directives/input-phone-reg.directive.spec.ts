import { InputPhoneRegDirective } from './input-phone-reg.directive';

describe('InputPhoneRegDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPhoneRegDirective();
    expect(directive).toBeTruthy();
  });
});
